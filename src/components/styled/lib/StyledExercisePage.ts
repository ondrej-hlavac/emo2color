import styled from 'styled-components';

export const StyledExercisePage = styled.article`
    height: 100vh;
    width: 100vw;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;

    .results-button {
      position: fixed;
      bottom: 0;
      right: 10px;
    }
`; 